import React from 'react';

/* eslint-disable-next-line */
export interface InterpretProps {}

export const Interpret = (props: InterpretProps) => {
  return (
    <div>
      <h1>Welcome to interpret!</h1>
    </div>
  );
};

export default Interpret;
